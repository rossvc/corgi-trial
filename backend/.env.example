# CORS allowed origins (comma-separated)
# For production, set to your frontend domain(s)
ALLOWED_ORIGINS=https://corgi-trial.ross.vc

# Data directory for GRIB files and GeoTIFFs
# DATA_DIR=/app/data

# Polling interval in seconds for fetching new radar data
# POLL_INTERVAL=10
